local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local RS = game:GetService("ReplicatedStorage")

local Player = Players.LocalPlayer
local ClaimTimedReward = RS:WaitForChild("ClaimTimedReward")


pcall(function()
	local old = Player.PlayerGui:FindFirstChild("RewardTester")
	if old then
		old:Destroy()
	end
end)


local Gui = Instance.new("ScreenGui")
Gui.Name = "Taylor safadinho 👀🍑💨"
Gui.ResetOnSpawn = false
Gui.DisplayOrder = 999
Gui.Parent = Player.PlayerGui


local Scale = Instance.new("UIScale")
Scale.Parent = Gui

if UIS.TouchEnabled and not UIS.KeyboardEnabled then
	Scale.Scale = 0.85
else
	Scale.Scale = 1
end

-- BOTÃO ABRIR

local Open = Instance.new("TextButton")
Open.Name = "OpenButton"
Open.Parent = Gui

Open.Size = UDim2.new(0,70,0,70)
Open.Position = UDim2.new(0,30,0.5,-35)

Open.Text = "🎁"
Open.TextSize = 35

Open.BackgroundColor3 = Color3.fromRGB(120,40,200)
Open.TextColor3 = Color3.new(1,1,1)


local OpenCorner = Instance.new("UICorner")
OpenCorner.Parent = Open
OpenCorner.CornerRadius = UDim.new(1,0)


local OpenStroke = Instance.new("UIStroke")
OpenStroke.Parent = Open
OpenStroke.Color = Color3.fromRGB(255,255,255)
OpenStroke.Thickness = 2



-- Frame

local Frame = Instance.new("Frame")
Frame.Parent = Gui

Frame.Size = UDim2.new(0,260,0,220)
Frame.AnchorPoint = Vector2.new(0.5,0.5)
Frame.Position = UDim2.new(0.5,0,0.5,0)

Frame.BackgroundColor3 = Color3.fromRGB(25,25,30)



local Corner = Instance.new("UICorner")
Corner.Parent = Frame
Corner.CornerRadius = UDim.new(0,12)



local Stroke = Instance.new("UIStroke")
Stroke.Parent = Frame
Stroke.Color = Color3.fromRGB(140,60,255)



-- titulo

local Title = Instance.new("TextLabel")
Title.Parent = Frame

Title.Size = UDim2.new(1,0,0,40)
Title.BackgroundColor3 = Color3.fromRGB(90,30,150)

Title.Text = "Reward Tester"
Title.TextColor3 = Color3.new(1,1,1)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 16



-- fechar

local Close = Instance.new("TextButton")
Close.Parent = Title

Close.Size = UDim2.new(0,40,1,0)
Close.Position = UDim2.new(1,-40,0,0)

Close.Text = "X"
Close.TextSize = 20

Close.BackgroundColor3 = Color3.fromRGB(180,40,50)
Close.TextColor3 = Color3.new(1,1,1)



Close.MouseButton1Click:Connect(function()
	Frame.Visible = false
	Open.Visible = true
end)



Open.MouseButton1Click:Connect(function()
	Frame.Visible = true
	Open.Visible = false
end)



-- letra

local Letter = Instance.new("TextBox")
Letter.Parent = Frame

Letter.Size = UDim2.new(0.8,0,0,35)
Letter.Position = UDim2.new(0.1,0,0.3,0)

Letter.Text = "A"
Letter.PlaceholderText = "Letra (A-L)"

Letter.TextSize = 16
Letter.BackgroundColor3 = Color3.fromRGB(45,45,50)
Letter.TextColor3 = Color3.new(1,1,1)



-- quantidade

local Amount = Instance.new("TextBox")
Amount.Parent = Frame

Amount.Size = UDim2.new(0.8,0,0,35)
Amount.Position = UDim2.new(0.1,0,0.55,0)

Amount.Text = "40"
Amount.PlaceholderText = "Quantidade"

Amount.TextSize = 16
Amount.BackgroundColor3 = Color3.fromRGB(45,45,50)
Amount.TextColor3 = Color3.new(1,1,1)



-- enviar

local Send = Instance.new("TextButton")
Send.Parent = Frame

Send.Size = UDim2.new(0.8,0,0,40)
Send.Position = UDim2.new(0.1,0,0.8,0)

Send.Text = "ENVIAR"
Send.TextSize = 16

Send.BackgroundColor3 = Color3.fromRGB(60,180,70)
Send.TextColor3 = Color3.new(1,1,1)



Send.MouseButton1Click:Connect(function()

	local letra = Letter.Text
	local qtd = tonumber(Amount.Text)

	if letra and qtd then

		ClaimTimedReward:FireServer(
			letra,
			qtd
		)

	end

end)



-- DRAG

local Dragging = false
local DragStart
local StartPos


Title.InputBegan:Connect(function(input)

	if input.UserInputType == Enum.UserInputType.MouseButton1
	or input.UserInputType == Enum.UserInputType.Touch then

		Dragging = true
		DragStart = input.Position
		StartPos = Frame.Position

	end

end)



UIS.InputChanged:Connect(function(input)

	if Dragging then

		if input.UserInputType == Enum.UserInputType.MouseMovement
		or input.UserInputType == Enum.UserInputType.Touch then


			local delta = input.Position - DragStart


			Frame.Position = UDim2.new(
				StartPos.X.Scale,
				StartPos.X.Offset + delta.X,
				StartPos.Y.Scale,
				StartPos.Y.Offset + delta.Y
			)

		end

	end

end)



UIS.InputEnded:Connect(function(input)

	if input.UserInputType == Enum.UserInputType.MouseButton1
	or input.UserInputType == Enum.UserInputType.Touch then

		Dragging = false

	end

end)
